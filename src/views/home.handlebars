<header><a href="/carts/674272d2251dc31ce00ab070" class="cart-icon">🛒</a></header>

<h1>Products</h1>
<div class="products-container">
  {{#if products.length}}
    {{#each products}}
      <div class="product-card">
        <a href="/products/{{this._id}}" class="product-link">
        <h2>{{this.title}}</h2>
        <p>Description: {{this.description}}</p>
        <p>Code: {{this.code}}</p>
        <p>Price: ${{this.price}}</p>
        <p>Status:
          <span class="{{#if this.status}}status-available{{else}}status-not-available{{/if}}">
            {{#if this.status}}Available{{else}}Not available{{/if}}
          </span>
        </p>
        <p>Stock: {{this.stock}}</p>
        <p>Category: {{this.category}}</p>
        </a>
      </div>
    {{/each}}
  {{else}}
    <p class="no-products">No products available</p>
  {{/if}}
</div>

<link rel="stylesheet" href="/css/stylesHome.css">
<script src="../public/js/products.js"></script>